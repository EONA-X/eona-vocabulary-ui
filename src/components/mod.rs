//! Component tree for the ontology browser UI.
//!
//! Empty for now — later stages add `atoms`, `molecules`, and `organisms`
//! submodules here as the Vue components under
//! `containers/prez-ui/theme/app/components/` are ported to Yew.
