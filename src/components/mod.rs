//! Component tree for the ontology browser UI.
//!
//! `atoms` holds the leaf presentational components; later stages add
//! `molecules` and `organisms` submodules here as the Vue components under
//! `containers/prez-ui/theme/app/components/` are ported to Yew.

pub mod atoms;
